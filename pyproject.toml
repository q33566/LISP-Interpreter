[project]
name = "lisp-interpreter"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ply>=3.11",
    "pytest>=8.3.5",
]
